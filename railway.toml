[build]
builder = "nixpacks"

[build.env]
NIXPACKS_PYTHON_VERSION = "3.10"

[deploy]
startCommand = "pip install -r backend/requirements.txt && cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
